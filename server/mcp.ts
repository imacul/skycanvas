import{McpServer}from"@modelcontextprotocol/sdk/server/mcp.js";import{StdioServerTransport}from"@modelcontextprotocol/sdk/server/stdio.js";import{z}from"zod";const server=new McpServer({name:"skycanvas-mcp",version:"0.1.0"});server.tool("get_design","Get a SkyCanvas design document from a design URL or id",{design:z.string()},async({design})=>({content:[{type:"text",text:JSON.stringify({design,instruction:"Fetch the SkyCanvas scene document from the configured SkyCanvas host. The document contains exact node geometry, typography, fills and hierarchy."})}]}));server.tool("get_design_context","Return implementation guidance for a SkyCanvas design",{design:z.string()},async({design})=>({content:[{type:"text",text:`Implement SkyCanvas design ${design} pixel-for-pixel. Preserve exact geometry, hierarchy, typography, fills, radii and assets. Use the scene graph as source of truth and screenshots for visual verification.`}]}));await server.connect(new StdioServerTransport());